{
  "name": "chapterlight",
  "private": true,
  "version": "0.6.4",
  "engines": { "node": ">=20" },
  "scripts": {
    "preview": "node tools/preview.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
